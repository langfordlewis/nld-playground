# 00 — Frontend Basics

This module is a structured playground to refresh and strengthen HTML, CSS, and basic JavaScript skills.  
Follow the exercises step by step and check them off as you complete them ✅.

---

## 📚 Phase 1 — HTML Foundations

- [x] Create a semantic page skeleton with:
  - `<header>`, `<main>`, `<footer>`
  - `<section>` blocks
  - `<article>` and `<aside>`
  - `<nav>` with links

- [x] Add text-level semantics:
  - `<p>`, `<strong>`, `<em>`, `<mark>`, `<abbr>`
  - `<blockquote>` and `<cite>`
  - `<time>` element

---

## 🎨 Phase 2 — CSS Foundations

- [ ] Set up basic styles:
  - `body` font, line-height, background, color
  - Headers and paragraphs

- [ ] Box model & spacing:
  - `margin`, `padding`, `border`, `box-shadow`
  - Create a card component

- [ ] Layout with Flexbox:
  - Horizontal nav bar
  - Card section layout

- [ ] Layout with Grid:
  - 2x2 or 3x3 grid of cards
  - Responsive adjustments with `grid-template-columns`

---

## 📱 Phase 3 — Responsive Design

- [ ] Media queries for small screens:
  - Collapsing nav
  - Grid layout → 1 column on mobile

- [ ] Fluid typography and spacing:
  - Use `clamp()`, `%`, or `rem` units

- [ ] Test across browser widths and document observations

---

## 📝 Phase 4 — Forms & Accessibility

- [ ] Build a small form with:
  - `<form>`, `<label>`, `<input>`, `<textarea>`, `<button>`

- [ ] Ensure accessibility:
  - `for` attribute links
  - Keyboard navigation
  - `<fieldset>` and `<legend>` usage
  - Basic validation (`required`, `type="email"`)

---

## ⚡ Phase 5 — Basic Interactivity (JS)

- [ ] DOM manipulation:
  - `querySelector`, `getElementById`
  - Change text, styles, attributes dynamically

- [ ] Event handling:
  - Button clicks changing content
  - Live feedback from inputs

- [ ] Document each JS method used and why

---

## 🎯 Phase 6 — Polish & Document the Journey

- [ ] Summarize learning:
  - What was practiced
  - Challenges encountered
  - Screenshots of layouts

- [ ] Commit all work and push to GitHub
- [ ] Add screenshots to `assets/images/` (optional)
